// ai-interviewer/web/config.js
// Safe to commit: the publishable key is protected by RLS (read-only), Vapi public key is designed for client-side use.
// PLACEHOLDER VALUES — replace each of these with your real project values before running the app.
const CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_PUBLISHABLE_KEY: "YOUR-PUBLISHABLE-KEY",
  VAPI_PUBLIC_KEY: "YOUR-VAPI-PUBLIC-KEY",
  N8N_BASE_URL: "https://YOUR-N8N-INSTANCE/webhook"
};
